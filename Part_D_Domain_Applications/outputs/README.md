# Part D Runtime Outputs

This folder keeps the Part-specific slice of the current-runtime domain adapter sweep. The release-wide cascade, full sweep, figures, and HTML panel index live under `Part_E_Synthesis/outputs/` and `Part_E_Synthesis/figures/` from the release root.

Read these files as model output and bookkeeping for later measurement, not as field evidence.
